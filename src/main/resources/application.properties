spring.application.name=coaching-manager

# =========================
# LOCAL CONFIG
# =========================
spring.datasource.url=jdbc:mysql://localhost:3306/coaching_manager
spring.datasource.username=root
spring.datasource.password=Lavanu#1

jwt.secret=mysecretkeymysecretkeymysecretkeymysecretkey


# =========================
# ONLINE / RENDER CONFIG
# =========================
# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}

# jwt.secret=${JWT_SECRET}


# =========================
# COMMON CONFIG
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.expiration=86400000